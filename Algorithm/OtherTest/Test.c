#include"Test.h"

int main()
{
	int tmp, num = 3;
	int base[100] = { 0 };
	int book[100] = { 0 };

	//BomberMan_1();
	//StickEquation();
	//FullPermutation_1();
	//FullPermutation_DFS(base, 0, book, num);
	//EquationEnum();
	//EquationArr();
	//tmp = EquationDFS(base, 0, book);
	//printf("count = %d\n", tmp);
	ExitMaze();

	system("pause");
	return 0;
}